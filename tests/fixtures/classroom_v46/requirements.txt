# Kept here so the skill remains self-contained when copied independently.
# The repository root requirements.txt is the canonical installer input.
PyMuPDF>=1.24.4,<2
python-pptx>=1.0,<2
Pillow>=10,<14
pdfplumber>=0.11,<1
numpy>=1.26,<3
